export function validateDeploymentInput(input:Record<string,unknown>={}){const forbidden=["replaceTesting","replaceCertification","fabricateDeploymentSuccess","bypassApprovalGates","deployUnvalidatedBuild","implementQ615OrLater","overridePillow","overrideGrandKing","overrideApprovedArchitecture"];const issues=forbidden.filter(key=>input[key]===true).map(key=>`${key} is forbidden`);if(input.missionId&&input.missionId!=="Q6-14")issues.push("missionId must be Q6-14");return {valid:issues.length===0,issues}}
