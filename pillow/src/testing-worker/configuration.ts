import { existsSync,readFileSync } from "node:fs"; import { join } from "node:path"; import type { TestingWorkerConfiguration } from "./types.js";
export const DEFAULT_TESTING_WORKER_CONFIGURATION:TestingWorkerConfiguration={enabled:true,timeoutMs:120000,neverFabricateSuccessfulTests:true,neverModifyUnrelatedProductionCode:true,neverReplaceDeployment:true,neverReplaceCertification:true,neverImplementQ614OrLater:true,neverOverridePillowGrandKingApprovedArchitecture:true};
export function buildTestingWorkerConfiguration(root?:string,overrides:Partial<TestingWorkerConfiguration>={}):TestingWorkerConfiguration {let file:Partial<TestingWorkerConfiguration>={};try{const p=root&&join(root,"config","testing-worker.config.json");if(p&&existsSync(p))file=JSON.parse(readFileSync(p,"utf8"));}catch{}const timeout=Number.parseInt(process.env.TESTING_WORKER_TIMEOUT_MS??"",10);return {...DEFAULT_TESTING_WORKER_CONFIGURATION,...file,...overrides,timeoutMs:Number.isFinite(timeout)&&timeout>0?timeout:(overrides.timeoutMs??file.timeoutMs??DEFAULT_TESTING_WORKER_CONFIGURATION.timeoutMs),neverFabricateSuccessfulTests:true,neverModifyUnrelatedProductionCode:true,neverReplaceDeployment:true,neverReplaceCertification:true,neverImplementQ614OrLater:true,neverOverridePillowGrandKingApprovedArchitecture:true};}
