# Checkpoint capture and held-case dispatch engineering — 2026-09-23

Legacy capture/verifier uses read-only original descriptors, new exclusive private destination, source/target identity and hash checks before and after SQLite integrity validation. The review found a target-mutation race; it was fixed with two real valid-SQLite replacement regressions. Independent seven-test suite passed. Mandatory Linux Product CI added, pending for this change. No production database was copied or imported. Pending RAM remains UNKNOWN, no quiescence or full live-state claim, no import authority.

Held-case dispatch is inactive by default and requires trusted server-owned policy verification. It pins source/workspace/service/case-step prompt/provider/model and uses immutable snapshots, explicit reviewed pricing and token bounds, and a durable reservation before a single adapter attempt. Duplicate attempts remain denied after reopen. Full reservations remain after success and uncertainty until independent final invoice reconciliation. Initial reviewer found retained verifier references could mutate prompts; frozen independent snapshots plus regression fixed it. Nine offline tests independently passed. This primitive is not wired to HTTP/ordinary provider execution and does not prove actual remote spending enforcement. No owner approval or pricing was invented.

Root validation preserved two pre-test tooling failures: integration has no local tsx package, then the explicit existing loader encountered Windows sandbox uv_os_get_passwd ENOMEM. The same offline command outside that sandbox failure passed all nine tests without skips. No package installation or actual provider calls occurred.

Vercel exception: initial isolated recovery4 branch commit1ff5d85afffd6941445fd68413817e6dc1e6d8c8 omitted its new branch-name preview exclusion. It produced preview dpl_GaZpv5QGbAztWxYUS1WQiFAtRxNN (41s build), confirmed in Vercel UI. Corrected immediately in8bde6e099109c1abfc3e8a8eaa55012e49a84276 before PR9; latest visible preview still points to initial1ff. Production remains dpl_7rfJqEeCpsojJ2nqF5V6jDevui8R/main21384342. Preview has not been invoked or promoted in this work. No delete performed without required confirmation.

Latest isolated Railway test project charge $0.0087 is provisional. Recovery3 compute remains stopped; permanent cleanup confirmation pending. Recovery4 branch PR9 is draft, never merge; actual services not yet created. Historical before/after recovery failures remain unaltered.
