# Founder UX (Vite) — API target for split-stack managed deployment
# Local: Brain on port 4000
VITE_API_BASE_URL=http://localhost:4000

# Production (Vercel): set in Vercel project env to your Railway Brain URL
# VITE_API_BASE_URL=https://your-brain-service.up.railway.app
