import type { RateLimitStatus } from "./types.js";
type Limit={maxRequests:number;windowMs:number;started:number;used:number};
export class RateLimitManager {private limits=new Map<string,Limit>();configure(integrationId:string,maxRequests:number,windowMs:number){if(maxRequests<1||windowMs<1)throw new Error("Rate limit values must be positive.");this.limits.set(integrationId,{maxRequests,windowMs,started:Date.now(),used:0});} take(integrationId:string){let x=this.limits.get(integrationId)??{maxRequests:100,windowMs:60_000,started:Date.now(),used:0};if(Date.now()-x.started>=x.windowMs)x={...x,started:Date.now(),used:0};const allowed=x.used<x.maxRequests;if(allowed)x.used++;this.limits.set(integrationId,x);return {...this.status(integrationId),allowed};} status(integrationId:string):RateLimitStatus{const x=this.limits.get(integrationId)??{maxRequests:100,windowMs:60_000,started:Date.now(),used:0};return {integrationId,allowed:x.used<x.maxRequests,remaining:Math.max(0,x.maxRequests-x.used),resetAt:new Date(x.started+x.windowMs).toISOString(),maxRequests:x.maxRequests,windowMs:x.windowMs};}}
