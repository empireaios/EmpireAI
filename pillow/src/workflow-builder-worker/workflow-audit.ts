export type WorkflowAuditEvent={eventId:string;timestamp:string;event:string;details:Record<string,unknown>}; export class WorkflowAudit {private events:WorkflowAuditEvent[]=[];append(event:string,details:Record<string,unknown>={}){const v={eventId:`wbw-evt-${crypto.randomUUID()}`,timestamp:new Date().toISOString(),event,details};this.events.push(v);return v}list(limit=50){return this.events.slice(-limit).reverse()}}
