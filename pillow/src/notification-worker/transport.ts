import type { Channel,NotificationTransport } from "./types.js";
export interface NotificationTransportAdapter extends NotificationTransport {}
export class InMemoryNotificationTransport implements NotificationTransport {private outcomes=new Map<string,{success:boolean;reason?:string}|Array<{success:boolean;reason?:string}>>();set(key:string,outcome:{success:boolean;reason?:string}|Array<{success:boolean;reason?:string}>){this.outcomes.set(key,outcome);return this}async send(input:{providerId?:string;channel:Channel;to:string;subject?:string;body:string}){const key=input.providerId??`${input.channel}:${input.to}`,value=this.outcomes.get(key)??this.outcomes.get(`${input.channel}:${input.to}`);if(!value)return {success:false,reason:"No configured transport outcome"};return Array.isArray(value)?(value.shift()??{success:false,reason:"No configured transport outcome"}):value}}
