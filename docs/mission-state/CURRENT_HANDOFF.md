# EmpireAI / Pillow active mission handoff

Updated 2026-09-26 UTC after independent GitHub, CI and Railway readback. Machine-readable companion: [CURRENT_HANDOFF.json](CURRENT_HANDOFF.json). Dated project history remains in [PILLOW_MISSION_STATE.md](../../PILLOW_MISSION_STATE.md), [NEXT_ACTIONS.md](NEXT_ACTIONS.md), draft [PR #5](https://github.com/empireaios/EmpireAI/pull/5) and the closed recovery [PR #9](https://github.com/empireaios/EmpireAI/pull/9). The abandoned desktop Work execution is not a source of current state.

## Owner mandate and truth state

King authorizes ordinary reversible engineering, CI, isolated canary work and previously approved cleanup. Continue autonomously. Owner-only stops are credentials/2FA, material new expenditure, irreversible production data without recovery, material business risk without mandate, and final authorization for the bounded real commercial pilot. No further historical V53 artifact exists; the 84-requirement [replacement specification](../governance/PILLOW_REPLACEMENT_CERTIFICATION_V1.json) governs engineering but grants no credit itself.

**Pillow NOT_BORN; Wave credit 0; commerce LOCKED; real pilot not authorized.** No real Amazon seller API call, paid LLM call, real order, sale or production promotion occurred during this recovery.

## Source, deployment and test evidence

| Item | Last verified state |
| --- | --- |
| Main | `21384342c401def948926904913840e63c18dff7` |
| Candidate | Draft, unmerged PR #5, branch `fix/pillow-integrated-closure-20260920`. `53e370ed7f793e845ad4e65b4b7cf8ec843c859a` repaired the two `9046a3d4` CI failures and passed [Product](https://github.com/empireaios/EmpireAI/actions/runs/36238300749), [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36238300758) and [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36238300753). Inspect the branch ref for later commits. |
| Fully verified candidate | `0cb86061bf654e417d6e3e0096b7170768452306` has exact-head hosted Node 22 [Product](https://github.com/empireaios/EmpireAI/actions/runs/36246048158), [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36246048180) and [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36246048156) passes. Earlier `e7bbc7d` in-process per-handle save, `ac947755` disk capture size, `24e68078` CJ stock, `7c0fca18` CJ truth, `663f9876` token isolation, `94ee670a` offer price, `d8b38d12` disk capture and `2600ed04` inventory routes also passed all three exact-head workflows. These are offline engineering proofs, not a hosted restart, safe data migration, Birth, production promotion or commerce approval. |
| Pre-recovery failed CI | On `9046a3d4`, [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36091363688) passed; [Product](https://github.com/empireaios/EmpireAI/actions/runs/36091363660) failed because the unpriced-model test did not authorize owner budgets; [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36091363682) failed because its mandatory executed test name no longer matched the priced timeout test. The latter ran and passed, but the gate could not find it. Both defects were repaired and verified by the three hosted `53e370ed` workflows above. |
| Shadow CEO storage checkpoint | `e752bf97` repairs Railway volume-root selection, centralizes the default Shadow CEO DB path, makes request-owner JSON writes atomic and refuses corrupt reads; all three hosted workflows passed. The follow-on `23fba652` routes the separate authority JSON store to the volume and makes its writes atomic and corrupt reads fail closed. Focused local tests passed (16/16, Node 24), backend typecheck passed and all three exact-head hosted Node 22 workflows passed. No production migration or deployment occurred. |
| Amazon seller listings checkpoint | `7f5493ae` implements one bounded Amazon US Listings Items API v2021-08-01 `searchListingsItems` page per call with seller ID binding, durable request spacing, sanitized SKU summaries and cursor, critical disk save and readback; all three hosted workflows passed. `445685b5` adds founder-only read/sync routes, owner-started worker continuation, safe status without tokens and failure pause; all three exact-head hosted workflows passed. No real seller-account proof, supplier stock, pricing or sourcing compliance. |
| Amazon seller-managed inventory checkpoint | `970bd26a` reads `fulfillmentAvailability` only for one seller-bound Amazon US page and requires a `DEFAULT` merchant-fulfilled quantity. FBA-only and missing availability cannot become seller stock receipts. The page/cursor and 1-second request gate are saved before a completed inventory claim. Local focused tests and typecheck passed. [Product](https://github.com/empireaios/EmpireAI/actions/runs/36240478115) and [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36240478066) passed; [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36240478075) failed an existing zero-wait SQLite two-process claim test (326/327 runtime tests passed). `005e9dbb` bounded the child fixture's busy retry and passed all three exact-head workflows. `2600ed04` added founder inventory read/sync/status, owner-started continuation, failure pause and current-cycle filtering for both seller listings and inventory; focused Node 24 tests passed (9/9), backend typecheck passed and all three exact-head hosted workflows passed. Provider proof, supplier stock and pricing remain pending. |
| Amazon seller offer price checkpoint | `94ee670a` requests `summaries,offers` through the existing bounded seller listing search. Only one same-marketplace B2C offer with a strictly parsed positive USD amount becomes integer cents; B2B and other marketplaces are ignored, missing offers are explicitly unpriced, ambiguous/invalid B2C offers fail the page. Pre-upgrade rows remain unpriced. Six focused local tests, backend typecheck and all three exact-head hosted workflows passed. This is a seller's current offer, **not** upstream supplier cost, margin, pricing permission or real provider proof. Amazon's [Listings Items reference](https://developer-docs.amazon.com/sp-api/reference/searchlistingsitems) and [offer response example](https://developer-docs.amazon.com/sp-api/docs/manage-product-listings-guide) define the requested data. |
| CJ token isolation checkpoint | `663f9876` binds the existing CJ access/refresh cache and concurrent authentication flights to an account credential and endpoint fingerprint and refuses missing/near-expired token expiry. Ten local authentication/proof tests, backend typecheck and all three exact-head hosted workflows passed. Official [authentication](https://developers.cjdropshipping.com/en/api/api2/api/auth.html) requires posting the key in the JSON body and using the resulting access token. |
| CJ legacy adapter truth checkpoint | `7c0fca18` corrects the separate live-commerce adapter's auth probe to POST `{apiKey}` and check the returned access token/expiry, while withholding production readiness until actual durable supplier receipts exist. Its four production sync methods fail closed without fabricating counts or calling a sample SKU. Thirteen focused offline tests, backend typecheck and all three exact-head hosted workflows passed. This does **not** implement CJ catalog, stock, cost, shipping or order operations; no live CJ request/API points consumed. |
| CJ supplier stock claim repair | `fb4aa70c` removes both presale and re-evaluation fallbacks from variant summary inventory, SKU queries, factory inventory and total stock. Only one exactly matched `queryByVid` response with valid warehouse country and nonnegative integer `cjInventoryNum` can provide a direct CJ-managed stock claim. Invalid/mixed/duplicate rows fail closed. The lookup never retries on a provider failure (one point-charging stock query maximum per candidate); local focused tests and backend typecheck pass. `24e68078` adds the exact VID test and presale integration to hosted Product CI. All three exact-head [Product](https://github.com/empireaios/EmpireAI/actions/runs/36244374844), [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36244374924), [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36244374780) passed. CJ docs distinguish [CJ-managed from factory inventory](https://developers.cjdropshipping.com/en/api/api2/api/product.html#3-inventory), and [API point schedule](https://developers.cjdropshipping.com/en/api/api2/standard/points.html) assigns 10 points per stock lookup. No real provider call or cost incurred by this engineering checkpoint. This repair does not prove supplier procurement, landed cost, compliance or any production receipt. |
| Legacy Shadow CEO disk capture | `d8b38d12` adds `deployment/legacy-shadow-state-capture.cjs`, CLI, instructions and a seven-case local proof to copy the three old-main disk stores onto the mounted volume with hashes, SQLite integrity and refusal on concurrent mutation, symlinks, bad JSON and SQLite sidecars. All three exact-head CI workflows passed; **no old production file has been copied**. Its manifest always records unknown SQL.js RAM, no quiescence and no cutover authority. See [capture procedure](../../deployment/legacy-shadow-state-capture.md). |
| Oversized persisted checkpoint capture | `ac947755` raises only the streaming standalone `legacy-checkpoint-capture.cjs` source bound from 512 MiB to 1 GiB. A 512 MiB-plus sparse but valid SQLite fixture was fully copied, hashed, integrity checked and verified; eight local tests and all exact-head hosted [Product](https://github.com/empireaios/EmpireAI/actions/runs/36244923445), [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36244923442) and [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36244923419) workflows passed. This makes the known 572,035,072-byte **persisted** checkpoint size eligible for an authorized disk copy, but still cannot read pending SQL.js RAM, stop writers, resolve Shadow CEO stores or authorize a production cutover. The separate offline-state-bundle tool still has its own 512 MiB bound. |
| In-process SQL.js save boundary | `e7bbc7d` adds a per-instance write fence across `exec`, prepared writes, mutating pragmas and close, and `withQuiescedSqliteSave` holds the fence while awaiting `requestCriticalPersist()` and a caller's verified disk capture. Local test starts with a record present only in RAM, captures through the running handle, verifies the copied SQLite row, rejects writes while fenced, resumes writes after success/failure; 12 focused local tests including persistence regressions and backend typecheck passed. Exact-head hosted [Product](https://github.com/empireaios/EmpireAI/actions/runs/36245547576) (explicit new test step passed), [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36245547534) and [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36245547539) passed. The fence does **not** stop other SQL.js instances, JSON stores, native DB, Redis, schedulers, network admission or other processes, and is **not present on old production**. No production action taken. |
| Large offline multi-store backup/restore | `0cb86061` raises the offline-state-bundle primary limit to 1 GiB for both backup and restore. A valid SQLite fixture just above 512 MiB was streamed into a bundle alongside real native mission/execution files, restored to a new path, integrity checked and queried for a preserved business row. Focused local test, backend typecheck and all three exact-head hosted [Product](https://github.com/empireaios/EmpireAI/actions/runs/36246048158), [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36246048180), [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36246048156) workflows passed. The tool still requires independent all-writer quiescence evidence, excludes RAM/Shadow CEO/Redis, and has **not** run on production. |
| Running-container file metadata | Railway Agent read-only `listContainerFilesTool` on 2026-09-26 listed `/data/empireai-brain.db` (572,035,072 bytes; modified 05:32:57 UTC), `/data/commissioning-mirror/ws_empire_1.json` and historic `/app/closure-backups` snapshots. `/app/backend/dist/orchestration/shadow-ceo-authority/repository` had JS/map only (no `data` entry); `/app/backend/src/.../repository/data` had `.gitignore` only. Agent reported `/app/.data` and `/app/backend/.data` missing. The actual process `cwd` remains **unknown** because `/proc/1/cwd` could not be resolved by this connector; these listings do **not** prove no other legacy state exists. No file contents of database, RAM or authority records were read. |
| Historic backup metadata | Agent's read-only text-file inspection found `/app/closure-backups/EmpireAI-db-20260921-manifest.json` declaring 8 chunks totaling 92,862,657 compressed bytes, and `EmpireAI-persisted-20260920.receipt.json` declaring a 569,380,864-byte persisted-only source snapshot compressed to 114,437,480 bytes at 2026-09-20 14:59 UTC. The receipt explicitly excludes pending RAM and Redis, with no tested restore. These ephemeral-container backups are not a verified current backup, quiesced live save or cutover evidence. |
| Importer intermediate failure | `a80df996df283ee1d1e3f62e289dfc0bbd9fd6ab` passed [Semantic](https://github.com/empireaios/EmpireAI/actions/runs/36088121250) and [Runtime](https://github.com/empireaios/EmpireAI/actions/runs/36088121246), failed [Product](https://github.com/empireaios/EmpireAI/actions/runs/36088121241) because its disk restart test called a helper that deletes the file. Current head uses close/reopen instead. |
| Railway live | Production status and deployment readback confirm old-main `21384342`, deployment `86302878-25b5-4990-87d9-86580cd4e976`, /data volume `40ab30d1-2759-4eba-b3c8-255436610bb0`. Fresh read-only 2026-09-26 12:50:26 UTC logs show ~4.1-second event-loop lag and `sqlite.pending=true`, `flushCount=3`, `flushInFlight=false`, no reported flush error and no critical flush requested/succeeded. Last reported flush `2026-09-26 05:32:57 UTC`; unsaved SQL.js RAM survival at restart remains **unproven**. An empty staged `EnvironmentPatch` (`changes: []`) remains; it was not accepted or altered. |
| Vercel live | Last authenticated-browser readback old-main deployment `dpl_7rfJqEeCpsojJ2nqF5V6jDevui8R`; 2026-09-26 read-only Vercel `list_teams` returned `teams=[]` and `get_deployment` using the known ID/team slug returned HTTP 403 Not authorized. The fresh browser redirected to sign-in. Fresh revision is **not verified**; no deployment changed. Historical observed API proxy 503s are in `VERCEL_BROWSER_VERIFICATION_2026-09-23.md`. |
| Canary cleanup | Owner committed eight approved deletions in isolated `empireai-canary-20260923` only. Railway readback: zero services, zero pending/staged work; project retained. |

## Implemented candidate scope, not deployed acceptance

- Railway-marked corrupt SQLite startup refuses empty replacement even when NODE_ENV is wrong; monthly AI spend counts toward operating cap; Cost Guard synthetic proof rolls back temporary limits/spend. The candidate paid LLM router requires short-lived exact-model authorized pricing and reserves a conservative budget on disk before each call. Provider invoice reconciliation and hosted paid invocation remain unproved.
- Amazon production generic catalog/inventory/pricing sync refuses fabricated receipts. Amazon seller OAuth consent uses application ID and state; code/refresh grants use URL-encoded LWA forms; founder/workspace/single-use/five-minute state guards added. Real seller authorization and application ID are absent.
- Amazon production generic HMAC webhook cannot claim an authentic order notification. The CI-passed `fca92e4d` order importer uses Orders API v2026-01-01, one bounded page per invocation, scoped sanitized order snapshots, a persisted cursor and critical SQL.js save, plus founder-scoped production-critical read/sync routes. Those bounded continuation changes through `8a04682b` passed all three offline CI workflows. CI-passed `a721d7d` recovers only a workspace/provider-matched durable vault reference after restart and treats a partial page as queued normal progress rather than a failed job. Its offline disk restart test requires the second page to complete from the persisted cursor/credential. No real seller account, hosted worker/restart proof, settled lifecycle, inventory/pricing importer or supplier compliance proof exists yet.
- The CI-passed Shadow CEO storage repair places the default database, request owners and separate authority JSON under the attached Railway volume, rejects off-volume root overrides and missing mounts, uses atomic file replacement, and refuses unreadable records. Existing old-main `.data` contents and authority records beside the module may reside solely in the old container; never treat a newly empty `/data/shadow-ceo` as a migrated state.

## Preserved failed attempts

Recovery2 before 35/35, after 37/38 authority preservation failed; no accepted pair. Recovery3 before 35/35, replacement build failed, no after/pair; Redis stop exceeded expiry. Recovery4 browser path expired and abandoned, PR #9 closed unmerged. Direct Railway cleanup accept-deploy returned INVALID_ARGUMENT; owner committed isolated dashboard change and provider readback verified empty. A040 notification-guard head failed all workflows at backend typecheck because the test did not narrow an optional recovery row; fixed at b908. A80 importer Product failure above remains preserved.

The 2026-09-25 PR #5 `9046a3d4` Product and Runtime CI failures above remain linked evidence. A detached desktop Work execution may have contained additional local changes, but only the listed remote branch and hosted evidence persisted; nothing was recovered by resuming that execution. The other ten GitHub branches were enumerated at recovery; `main` remains protected and unchanged, while `codex/bounded-recovery4-20260923` still points to the closed, unmerged PR #9 head `8bde6e09`.

A repository-wide historical `git grep` against old main stalled for over 30 seconds at 12:52 UTC and was interrupted. Narrow `git show` of the exact old-main SQLite source and targeted `rg` searches succeeded; no conclusion depends on the stalled search.

`970bd26a` Runtime CI failed at `two separate worker processes cannot both claim one job`: an assertion expected one successful claim but observed zero during zero-wait SQLite contention. Product and Semantic passed on that exact code head. The isolated child test fixture at `005e9dbb` retries a busy claim for up to two seconds (less than the five-second lease), matching normal worker polling; targeted local test and all three exact-head hosted workflows passed. Preserve the red run alongside the repair receipt.

Production Railway variable names include `DATABASE_PATH` and `RAILWAY_VOLUME_MOUNT_PATH`, but neither `SHADOW_CEO_DATA_DIR` nor `EMPIRE_DATA_DIR`. Old-main code defaults its Shadow CEO database and request-owner JSON to `cwd/.data`, and authority JSON under the executing module's `data` directory, rather than the attached `/data` volume. Actual process cwd and old store contents are **unknown**, even after the path listings above. Preserve both legacy locations and require a quiesced migration/backup readback before deploying a path change; do not initialize an empty authority store as proof of continuity.

Production's `sqlite.pending=true` continued at 12:50 UTC, over seven hours after the last reported flush. The old process may hold unexported RAM writes. Old-main `backend/src/brain/sqlite-database.ts` skips overdue ordinary exports unless recent and smoothed lag are under its 100/200 ms idle gates; sustained ~4-second lag plausibly prevents progress. This is a source-and-log **inference**, not proof of the contents of RAM. Do not restart or deploy over it to collect disk files: first establish an authorized live-process quiesce and save/readback plan. No `requestCriticalPersist()` was reported, and the ordinary listing connector cannot invoke it.

At 13:04 UTC, read-only Railway deployment logs recorded old-process presale cycle `SMART_VIABLE_BATCH_COMPLETE` (24 retrieved, 19 rejected, next discovery page 44, no approval ID). This activity postdates the 05:32 disk checkpoint, increasing the need to preserve current RAM. The log does not establish that this cycle reached disk, spent provider points, or executed real commerce; no external call was made by the recovery agent.

The old main exposes `persistDatabase()` only inside the process; its SIGTERM/SIGINT handler persists as part of shutdown and **exits**, so that is not an authorized non-disruptive capture path. Railway officially supports `railway ssh` into the existing deployment with a registered SSH key; `railway shell` and `railway run` execute locally with variables and cannot read that process's RAM. This workspace has no authenticated Railway CLI/SSH session. The separate read-only Railway Agent capability query at approximately 13:05 UTC stalled for over 40 seconds and was terminated without result; do not wait for that mechanism indefinitely. An SSH session could inspect cwd/files and copy disk state, but a separate safe in-process pause and critical SQL.js flush is still required before claiming complete live state. Do not issue a signal, attach a debugger, run an in-container command or copy files against production until the method is validated outside production and its effect is reviewed.

The published candidate `ac947755` raises the streaming standalone checkpoint copy bound to 1 GiB with an outside-production 512 MiB-plus proof. The separate `0cb86061` raises offline-state-bundle's primary backup/restore bound to 1 GiB with an outside-production copy/readback test. Neither tool has run on old production; a volume snapshot or SSH disk copy still omits unknown unsaved RAM.

## Next executable actions

1. Complete the non-production **whole-application** quiescence boundary: deny new mutating admission, drain workers, fence every SQL.js handle and JSON store, await critical save of RAM and validate all copied files including native mission/execution state; demonstrate multi-store restart/readback in an isolated process. `e7bbc7d` proves one candidate SQL.js handle; `0cb86061` proves only a separately quiesced oversized offline disk bundle. Obtain a secure authorized in-container/in-process mechanism for the old deployment without a restart; Railway file listing and a disk copy cannot save its RAM. Do not apply the candidate code to the protected old process by redeployment. The independent-process Amazon order cursor proof already passed Product on [intermediate `d05f728`](https://github.com/empireaios/EmpireAI/actions/runs/36091201021), with fake transport only.
2. Implement a bounded real CJ variant-level supplier stock/cost query and fulfilment compliance evidence with durable readback. Keep auth token account-bound, request gates and provider points budgeted; no live CJ request or API point expenditure has been authorized here. Seller-managed availability and an Amazon listing offer are not supplier stock, margin or authorization to fulfill an order.
3. Add provider invoice reconciliation for conservative paid-LLM reservations; strengthen authenticated phone owner flow and Birth single-source authority.
4. Safely quiesce old production writers, prove pending SQL.js save and multi-store backup/readback before migration. Run a **fresh**, fixed-expiry hosted canary only with verified incremental cost under the previous US$5 bound; never reuse expired recovery4 markers.
5. Integrate review → CI → Railway/Vercel and verify deployed revision, runtime restart, commerce lifecycle, 84 independent certification requirements and unattended operation. Only then present the bounded real pilot for separate owner authorization.

Code, CI, hosted recovery, deployment, certification and real reconciled commercial transactions are separate claims.
