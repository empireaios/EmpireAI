import type { WorkflowRun,StepExecutionRecord } from "./types.js"; export class RunStore {private runs=new Map<string,WorkflowRun>();private records:StepExecutionRecord[]=[];save(r:WorkflowRun){this.runs.set(r.runId,r);return r}get(id:string){return this.runs.get(id)}list(){return [...this.runs.values()]}record(r:StepExecutionRecord){this.records.push(r);return r}getRecords(id?:string){return id?this.records.filter(x=>x.runId===id):[...this.records]}}
