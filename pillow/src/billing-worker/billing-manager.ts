import { BillingRuntime } from "./billing-runtime.js"; import { BillingStore } from "./billing-store.js"; import { buildBillingReport } from "./billing-builder.js"; import { validateBillingInput } from "./billing-validator.js"; import type { BillingBuildReport,BillingWorkerDependencies,BillingWorkerInput } from "./types.js";
export class BillingManager { readonly runtime=new BillingRuntime(); readonly store=new BillingStore(); private deps:BillingWorkerDependencies={}; private requirementsReportId:string|null=null; private architectureReportId:string|null=null; bindIntegrations(deps:BillingWorkerDependencies){this.deps={...this.deps,...deps};} receiveApprovedRequirementsReports(input:BillingWorkerInput={}){this.assert(input);this.requirementsReportId=input.requirementsReportId??"blw-req-received";return this.produce("requirements_received");} receiveApprovedRequirementsReport(input:BillingWorkerInput={}){return this.receiveApprovedRequirementsReports(input)} receiveApprovedArchitectureReports(input:BillingWorkerInput={}){this.assert(input);this.architectureReportId=input.architectureReportId??"blw-arch-received";return this.produce("architecture_received");} receiveApprovedArchitectureReport(input:BillingWorkerInput={}){return this.receiveApprovedArchitectureReports(input)} manageBillingAccounts(input:BillingWorkerInput={}){this.assert(input);return this.produce("manage_billing_accounts");} createSubscriptionPlans(input:BillingWorkerInput={}){this.assert(input);return this.produce("create_subscription_plans");} supportRecurringBilling(input:BillingWorkerInput={}){this.assert(input);return this.produce("support_recurring_billing");} generateInvoices(input:BillingWorkerInput={}){this.assert(input);return this.produce("generate_invoices");} trackInvoiceLifecycle(input:BillingWorkerInput={}){this.assert(input);return this.produce("track_invoice_lifecycle");} coordinatePaymentProviderWorkflows(input:BillingWorkerInput={}){this.assert(input);return this.produce("coordinate_payment_provider_workflows");} recordBillingTransactions(input:BillingWorkerInput={}){this.assert(input);return this.produce("record_billing_transactions");} handleRefundsAndCreditNotes(input:BillingWorkerInput={}){this.assert(input);return this.produce("handle_refunds_and_credit_notes");} produceCompleteBillingAuditHistory(input:BillingWorkerInput={}){this.assert(input);return this.produce("produce_billing_audit_history");} produceBillingBuildReport(input:BillingWorkerInput={}){this.assert(input);return this.produce("produce_billing_build_report");} submitReport(input:BillingWorkerInput={}){const report=this.produceBillingBuildReport(input);const result=this.deps.executiveReportingRuntime?.submitWorkerReport?.({missionId:"Q6-09",report});const candidate=(result as {records?:Array<{reportId?:string}>}|undefined)?.records?.[0]?.reportId??null;report.submittedToExecutiveReporting=Boolean(candidate);report.executiveReportId=candidate;return report;} private produce(step:string){const report=buildBillingReport(this.runtime,{requirementsReportId:this.requirementsReportId,architectureReportId:this.architectureReportId,buildSteps:[step]});return this.store.add(report);} private assert(input:BillingWorkerInput){const result=validateBillingInput(input);if(!result.valid)throw new Error(result.errors.join("; "));} }
