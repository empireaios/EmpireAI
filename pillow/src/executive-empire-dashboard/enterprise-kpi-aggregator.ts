/** Structural-only enterprise KPI summarization boundary. */
export class EnterpriseKpiAggregator {
  aggregate() { return "Enterprise KPIs: structural signal available; sensitive values masked."; }
}
