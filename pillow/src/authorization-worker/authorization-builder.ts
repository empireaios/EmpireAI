import { AUTHORIZATION_WORKER_IDENTITY, AUTHORIZATION_WORKER_REPORT_VERSION, AZW_METADATA_VERSION } from "./paths.js";
import type { AuthorizationBuildReport, AuthorizationDecision, AuthorizationWorkerInput } from "./types.js";
export class AuthorizationBuilder {
  create(input: AuthorizationWorkerInput): AuthorizationBuildReport { return { buildId: `azw-bld-${Date.now()}`, timestamp: new Date().toISOString(), platformId: input.platformId ?? null, platformName: input.platformName ?? null, rolesImplemented: [], permissionsImplemented: [], policiesImplemented: [], protectedResources: [], accessEvaluationResults: [], auditIntegrationStatus: "implemented", testResults: [], outstandingIssues: [], buildStatus: "draft", confidenceScore: 0, metadataVersion: AZW_METADATA_VERSION, requirementsReportId: input.requirementsReportId ?? null, architectureReportId: input.architectureReportId ?? null, factoryMissionId: input.factoryMissionId ?? null, businessId: input.businessId ?? null, businessObjective: input.businessObjective ?? null, canonicalAuthzReference: "backend/src/auth/permissions.ts", defaultDeny: true, leastPrivilege: true, neverAuthenticateUsers: true, neverReplaceAuthenticationWorker: true, neverImplementQ609OrLater: true, neverOverridePillow: true, neverOverrideGrandKing: true, neverOverrideApprovedArchitecture: true, followApprovedRequirementsAndArchitecture: true, preserveCompleteTraceability: true, separateAuthenticationFromAuthorization: true, preventPrivilegeEscalation: true, preserveAuditHistory: true, maskSensitiveValues: true, buildSteps: [], selfReviewPassed: false, selfReview: [], workerId: AUTHORIZATION_WORKER_IDENTITY.workerId, reportVersion: AUTHORIZATION_WORKER_REPORT_VERSION, traceabilityRefs: [], submittedToExecutiveReporting: false, executiveReportId: null }; }
  stage(report: AuthorizationBuildReport, step: string) { report.buildStatus = "in_progress"; report.buildSteps = [...new Set([...report.buildSteps, step])]; report.testResults = [...new Set([...report.testResults, `runtime:${step}`])]; return report; }
  populate(report: AuthorizationBuildReport, roles: string[], permissions: string[], policies: string[], decisions: AuthorizationDecision[]) { report.rolesImplemented = roles; report.permissionsImplemented = permissions; report.policiesImplemented = policies; report.protectedResources = [...new Set(decisions.map((item) => item.resource))]; report.accessEvaluationResults = decisions.map(({ decision, reason, resource, action }) => ({ decision, reason, resource, action })); }
  complete(report: AuthorizationBuildReport) { report.buildStatus = "complete"; report.selfReviewPassed = Boolean(report.requirementsReportId && report.architectureReportId); report.selfReview = report.selfReviewPassed ? ["requirements traced", "architecture traced", "default deny verified", "authentication remains external"] : ["requirements and architecture report IDs required"]; report.outstandingIssues = report.selfReviewPassed ? [] : ["Missing approved requirements or architecture reference"]; report.confidenceScore = report.selfReviewPassed ? 100 : 65; report.traceabilityRefs = [`requirements:${report.requirementsReportId}`, `architecture:${report.architectureReportId}`]; return report; }
}
